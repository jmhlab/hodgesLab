---
layout: base.njk
permalink: /
structuresOnPage: true
hideRecruit: true
---
{% set heroStruct = structures[site.hero.structure] %}
<section class="hero">
  <div class="hero-bg{% if site.hero.photo %} has-photo{% endif %}" aria-hidden="true">
    {% if site.hero.photo %}
    <picture>
      {% if site.hero.photo_phone %}<source media="(max-width: 700px)" srcset="{{ ('/assets/img/hero/' ~ site.hero.photo_phone) | url }}">{% endif %}
      <img class="hero-photo" src="{{ ('/assets/img/hero/' ~ site.hero.photo) | url }}" alt="" style="--focus: {{ site.hero.photo_focus or '50% 50%' }}; --focus-phone: {{ site.hero.photo_focus_phone or site.hero.photo_focus or '50% 50%' }};">
    </picture>
    {% else %}
    <div class="structure" data-structure="{{ heroStruct.file }}" data-view="{{ heroStruct.view }}" data-ortho="{{ heroStruct.ortho }}" data-spin="{{ heroStruct.spin }}" data-alpha="{{ heroStruct.alpha or 0.85 }}" data-scale="{{ heroStruct.scale or '' }}" data-cell="0" data-fit="2.6" data-structure-alt="{{ heroStruct.alt or '' }}" data-labels="{{ heroStruct.labels or '' }}" data-hold="{{ heroStruct.hold or 4 }}" data-fade="{{ heroStruct.fade or 1.2 }}" data-label-target="#hero-temp"></div>
    {% endif %}
  </div>
  <div class="hero-inner">
    <div class="kicker">{{ site.hero.kicker }}</div>
    <h1><span class="h-lead">{{ site.hero.line1 | replace("|", " <br class=\"ph\">") | safe }}</span><br><span class="h-sub">{{ site.hero.line2 | replace("|", " <br class=\"ph\">") | safe }}</span></h1>
    <p>{{ site.hero.blurb | trim }}</p>
    <div class="hero-actions">
      <a class="btn btn-amber" href="{{ '/research/' | url }}">Research</a>
      <a class="btn" href="{{ '/publications/' | url }}">Publications</a>
    </div>
  </div>
  {% if not site.hero.photo or site.hero.photo_caption %}<div class="hero-caption">{% if site.hero.photo %}{{ site.hero.photo_caption }}{% else %}{{ heroStruct.caption or heroStruct.label }}{% if heroStruct.alt %} · <span id="hero-temp" style="color: var(--amber);"></span>{% endif %} · rendered from the group's crystal structures{% endif %}</div>{% endif %}
</section>

<section class="about">
  {% for block in site.about %}
  <div class="about-col">
    <h2 class="about-heading">{{ block.heading }}</h2>
    <div class="about-body">{% for para in block.body | trim | split("\n\n") %}<p>{{ para | replace("\n", " ") | em | safe }}</p>{% endfor %}</div>
    <div class="col-tiles">
      {% for p in pillars | where("under", block.key) %}
      <a class="pillar" href="{{ p.link | url }}">
        <div class="pillar-icon">{% set icon = p.icon %}{% include "pillar-icon.njk" %}</div>
        <div class="pillar-text">
          <h3 class="pillar-title">{{ p.title }}</h3>
        </div>
      </a>
      {% endfor %}
    </div>
  </div>
  {% endfor %}
</section>

{# Selected papers: numbers from publications.yml, listed in site.yml → featured_papers.
   Falls back to the three most recent while the list is empty. #}
{% set picks = [] %}
{% for n in site.featured_papers %}{% set pp = publications | find("n", n) %}{% if pp %}{% set picks = (picks.push(pp), picks) %}{% endif %}{% endfor %}
{% if picks | length == 0 %}{% set picks = publications | take(3) %}{% endif %}
<section class="reading">
  <div class="section-head">
    <h2 class="eyebrow">Papers to read</h2>
    <a class="more" href="{{ '/publications/' | url }}">All publications →</a>
  </div>
  <div class="reading-list">
    {% for pp in picks %}
    <a class="reading-item" href="{{ pp.url or ('https://doi.org/' ~ pp.doi) }}">
      <div class="reading-title">{{ pp.title | chem | safe }}</div>
      <div class="reading-cite">{{ pp.authors | boldMembers(site.member_names) | safe }} — <em>{{ pp.journal }}</em>, {{ pp.year }}</div>
    </a>
    {% endfor %}
  </div>
</section>
